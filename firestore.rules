rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /lesson_register_store/{document} {
      // Allows anyone to read and write records to store timetables, students, logs, and subs.
      // This is perfect for a shared administration register.
      allow read, write: if true;
    }
  }
}
